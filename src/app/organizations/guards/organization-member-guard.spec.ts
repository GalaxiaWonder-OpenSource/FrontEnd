import { OrganizationMemberGuard } from './organization-member-guard';

describe('OrganizationMemberGuard', () => {
  it('should create an instance', () => {
    expect(new OrganizationMemberGuard()).toBeTruthy();
  });
});
