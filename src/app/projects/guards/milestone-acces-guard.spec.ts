import { MilestoneAccesGuard } from './milestone-acces-guard';

describe('MilestoneAccesGuard', () => {
  it('should create an instance', () => {
    expect(new MilestoneAccesGuard()).toBeTruthy();
  });
});
