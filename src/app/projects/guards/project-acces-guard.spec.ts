import { ProjectAccesGuard } from './project-acces-guard';

describe('ProjectAccesGuard', () => {
  it('should create an instance', () => {
    expect(new ProjectAccesGuard()).toBeTruthy();
  });
});
